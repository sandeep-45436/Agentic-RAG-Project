export interface UniversityFaculty {
  id: string;
  name: string;
  email: string;
  departmentId: string;
  departmentCode: string;
  title: string;
  officeRoom?: string;
  organizationId: string;
}
